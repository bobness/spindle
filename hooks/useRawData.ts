const useRawData = () => {
  // TODO: useEffect() to get data from server
  return [
    { key: "Column1", values: [1, 2, 3] },
    { key: "Column2", values: [4, 5, 6] },
  ];
};

export default useRawData;
